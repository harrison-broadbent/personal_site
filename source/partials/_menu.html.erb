<div class="hidden sm:flex flex-col gap-x-4 gap-y-1 mt-auto mb-8 p-3 border border-stone-500 rounded-sm text-right">
  <% config.menu_pages.each do |name, path|%>
    <%= link_to name, path, class: "w-max text-stone-800" %>
  <% end%>
</div>
