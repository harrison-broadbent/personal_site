<ul>
  <% blog.articles.each do |article| %>
    <li class="flex">
      <time class="block w-32">[<%= article.date.strftime('%b %e %Y') %>]</time>
      <%= link_to article.title, article.url %>
    </li>
  <% end %>
</ul>
